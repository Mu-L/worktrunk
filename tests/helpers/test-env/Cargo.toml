[package]
name = "wt-test-env"
description = "Pre-main hermetic environment floor for worktrunk's test binaries"
edition = "2024"
rust-version = "1.96"
publish = false

[dependencies]
ctor = "1.0"

[lib]
name = "wt_test_env"
path = "src/lib.rs"
